# AI Literacy Project

---

## 2025_12_9

### 구구단

- 작성 프롬프트
  - python 프로그래밍을 하는데, 가장 기초적인 구구단을 출력하는 코드를 만들어줘
  - main 함수를 만들어서 entry point로 정하고 구구단을 함수로 만들어서 main 함수에서 호출을 해서 실행하는 구조로 만들어줘
  - print_gugudan() 함수에 디폴트 아규먼트 를 넣어서 dan 을 조절하게 하고 dan 의 디폴트는 9단으로 해줘, 그리고 타입을 힌트를 명시해서 명확하게 코드를 만들어줘
  - 이 구구단 함수를 클래스의 메소드로 만들어서 인스턴스변수를 활용해서 더 많은 기능을 가질 수 있게 구조를 만들어줘

## 2025_12_10

- 작성 프롬프트
  - 이 파이썬 파일을 실행하면 *가 피라미드 형식으로 터미널에 표시하게 만들어줘
  - '#' 문자를 써서 사각형을 터미널에 프린트하는 코드를 만들어줘
  - 판다스 패키지를 설치해서 쓰려고 한다. 간단한 데모코드를 작성해줘
  - agent 모드 에 대한 지침을 한글로 만들어줘. 일반적으로 쓰이는 python 프로젝트에서 쓸꺼야
  - 여러 파일을 다루고 서로 모듈로서 접근하는 방식도 지정하고, 문서를 만들어서 운용하는 방식도 지정해줘 changeRog.md  와 ReadME.md 그리고 Project.md 를 운영해줘.
  - 이 csv 파일을 분석해서 얻을수 있는 다양한 insight 를 정리해서 txt 파일로 내보내고 시각화 할 수 있는 데이터는 시각화를 해서 그림파일로 내보내줘 결과는 result 라는 폴더를 만들어서 로그파일 형식으로 저장해줘 ( csv 파일 첨부됨 )
  - 결과를 확인 했을때 sentiment_distribution.png 이결과에서 한글 폰트가 깨져 있다. 한글이 정상적으로 표현 될 수 있게 수정해줘

```text
 "chat.useAgentsMdFile": true,
```

- 이 심장 마비 관련한 데이터를 분석하려고 한다. 각 컬럼에 대해서 유추해서 의미를 정하고 상관관계표를 작성해줘, 그리고 시각화를 heatmap 으로 해서 그림파일로 저장해줘, result 폴더에 결과를 저장해줘
- 이 데이터의 cp 컬럼과 thalachh 컴럼 사이의 관계를 알고 싶다. thalachh을 기준으로 cp를 예측하는 작업을 수행하려고 한다. 머신러닝 기법중에 하나인 선형회귀로 계수를 계산하고 만들어진 모델로 예측한 결과와 cp 의 값을 비교 하려고 한다. 오차 컬럼도 만들어줘 그리고 scatter plot 으로 데이터를 시각화 하고 모델에 의해서 만들어지는 결과도 선으로 표현해줘. 오차 지표도 계산해서 알려줘
- 클래스가 컬럼명을 받아서 선형 회귀를 실행 할 수 있게 수정해주고 default 를 age 를 넣으면 thalachh 를 예측할 수 있게 수정해줘

## 2025_12_11

- fastapi 라이브러리를 사용해서 html 문서를 웹에 보여줘
html 문서는 index.html 문서를 따로 만들어서 보여주고 css 도 적용해서 css 파일을 만들어서 적용해줘 내용은 css가 충분히 demo로 보여 줄 수 있게 다양한 형태의 태그를 사용해줘
- [안내] : fastapi, uvicorn, jinja2 설치 필요
- data_analysis_fastapi 폴더를 새로 만들어서
그 안에서 uv 가상환경으로 fastapi 라이브러리를 사용해서 html 문서를 웹에 보여줘
html 문서는 index.html 문서를 따로 만들어서 보여주고 css 도 적용해서 css 파일을 만들어서 적용해줘 내용은 css가 충분히 demo로 보여 줄 수 있게 다양한 형태의 태그를 사용해줘
- uvicorn 실행을 코드 안에 넣고 실행 방법을 파이썬 파일을 실행하면 작동하도록 수정해줘
- html 에 메뉴 마다 따로 html 파일을 만들어서 관리해줘 공통적으로 들어가는 코드도 따로 default로 관리해줘
- 지금이 데모코드에 데이터베이스를 붙여서 ORM 을 통해서 또 다른 데모 페이지를 만들려고 한다.
SQLite 와 SQLMODEL 을 써서 구현해줘
그리고 기능은 채팅서버를 구현해줘. 그리고 채팅의 내용을 데이터베이스에 저장해줘
유저의 이름은 넣는 창을 만들고 유정 이름이 없으면 채팅을 할 수 없게 해줘
테이블의 컬럼은 유저, 시간, 채팅내용 으로 구성하고,
화면에 보이는 것은 한상 데이터 테이블을 참조해서 화면에 보여줘, 그리고 참조할 때는 40개의 데이터만 참조하도록 코드를 작성해줘
- 채팅 기능이 제대로 구현이 안되어 있다. 채팅 메세지는 일반적으로 새로운 메세지가 아래에 생기고 오래된 메세지는 위쪽으로 올라가게 만들고 메세지창은 가장 아래쪽에 위치시켜줘. 유저의 이름은 한번 입력하면 유지를 해주고, 메시지입력에서 엔터를 치면 클릭을 하지 않더라도 전송해줘 그리고 원래 엔터의 기능을 shit+엔터 키로 수정해줘
